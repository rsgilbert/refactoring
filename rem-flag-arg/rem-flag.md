# REMOVE FLAG ARGUMENT

Replace a single function that uses a flag argument 
for control flow with multiple functions that are 
explicit and have explicit names that indicate what they are doing. These multiple functions do not have the flag argument.